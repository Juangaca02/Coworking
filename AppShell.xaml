<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:TFG"
    xmlns:pages="clr-namespace:TFG.Pages"
    x:Class="TFG.AppShell">

    <FlyoutItem Title="Menu">
        <ShellContent Title="Salas"
                        ContentTemplate="{DataTemplate views:MainPage}" />
        <ShellContent Title="Historial"
                        ContentTemplate="{DataTemplate pages:HistorialPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Login" >
        <ShellContent Title="Login"
                        ContentTemplate="{DataTemplate pages:LoginPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Register">
        <ShellContent Title="Register"
                 ContentTemplate="{DataTemplate pages:RegisterPage}" />
    </FlyoutItem>

</Shell>
